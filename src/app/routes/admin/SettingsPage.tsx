import { PlaceholderView } from '@/app/routes/admin/PlaceholderView'
import { t } from '@/shared/i18n'

export function SettingsPage() {
  return (
    <PlaceholderView
      title={t.shell.pages.settings.title}
      body={t.shell.pages.settings.body}
    />
  )
}
