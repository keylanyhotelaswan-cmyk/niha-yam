import { PlaceholderView } from '@/app/routes/admin/PlaceholderView'
import { t } from '@/shared/i18n'

export function ChangePasswordPage() {
  return (
    <PlaceholderView
      title={t.shell.pages.changePassword.title}
      body={t.shell.pages.changePassword.body}
    />
  )
}
