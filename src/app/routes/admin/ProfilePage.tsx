import { PlaceholderView } from '@/app/routes/admin/PlaceholderView'
import { t } from '@/shared/i18n'

export function ProfilePage() {
  return (
    <PlaceholderView
      title={t.shell.pages.profile.title}
      body={t.shell.pages.profile.body}
    />
  )
}
